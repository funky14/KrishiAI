<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>net9.0-android36.0</TargetFrameworks>
		<!-- Uncomment to also build the tizen app. You will need to install tizen by following this: https://github.com/Samsung/Tizen.NET -->
		<!-- <TargetFrameworks>$(TargetFrameworks);net9.0-tizen</TargetFrameworks> -->

		<!-- Note for MacCatalyst:
		The default runtime is maccatalyst-x64, except in Release config, in which case the default is maccatalyst-x64;maccatalyst-arm64.
		When specifying both architectures, use the plural <RuntimeIdentifiers> instead of the singular <RuntimeIdentifier>.
		The Mac App Store will NOT accept apps with ONLY maccatalyst-arm64 indicated;
		either BOTH runtimes must be indicated or ONLY macatalyst-x64. -->
		<!-- For example: <RuntimeIdentifiers>maccatalyst-x64;maccatalyst-arm64</RuntimeIdentifiers> -->

		<OutputType>Exe</OutputType>
		<RootNamespace>KrishiAI.App</RootNamespace>
		<UseMaui>true</UseMaui>
		<SingleProject>true</SingleProject>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>

		<!-- Disable auto-generation of assembly attributes to prevent duplicates -->
		<GenerateAssemblyInfo>false</GenerateAssemblyInfo>
		<GenerateTargetFrameworkAttribute>false</GenerateTargetFrameworkAttribute>

		<!-- Display name -->
		<ApplicationTitle>KrishiAI</ApplicationTitle>

		<!-- App Identifier -->
		<ApplicationId>com.krishiai.farmerassistant</ApplicationId>

		<!-- Versions -->
		<ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
		<ApplicationVersion>1</ApplicationVersion>

		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">14.2</SupportedOSPlatformVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">24.0</SupportedOSPlatformVersion>

		<!-- Disable R8 minification for Debug builds to avoid assembly compression -->
		<EnableR8>false</EnableR8>
		<UseProguard>false</UseProguard>

		<!-- Ensure assemblies are stored uncompressed in APK -->
		<AndroidCreatePackagePerAbi>false</AndroidCreatePackagePerAbi>
		<AndroidPackageFormat>apk</AndroidPackageFormat>

		<!-- Embed assemblies into APK by default for stability -->
		<EmbedAssembliesIntoApk>true</EmbedAssembliesIntoApk>
		<AndroidUseAapt2>true</AndroidUseAapt2>
		<AndroidEnableMultiDex>true</AndroidEnableMultiDex>
		<AndroidDexTool>d8</AndroidDexTool>
		<AndroidLinkMode>None</AndroidLinkMode>
		<!-- Disable fast deployment override -->
		<AndroidEnableFastDeployment>false</AndroidEnableFastDeployment>
	</PropertyGroup>

	<!-- Debug configuration specific settings -->
	<PropertyGroup Condition="'$(Configuration)' == 'Debug'">
		<!-- Enable Fast Deployment for faster iteration in Debug builds -->
		<AndroidEnableFastDeployment>true</AndroidEnableFastDeployment>
		<!-- Disable embedding to keep APK small for incremental deployments -->
		<EmbedAssembliesIntoApk>false</EmbedAssembliesIntoApk>
		<EnableR8>false</EnableR8>
		<UseProguard>false</UseProguard>
		<AndroidLinkMode>None</AndroidLinkMode>
		<!-- Ensure assemblies are not compressed -->
		<AndroidPackageFormat>apk</AndroidPackageFormat>
		<AndroidUseAapt2>true</AndroidUseAapt2>
		<AndroidEnableMultiDex>true</AndroidEnableMultiDex>
		<AndroidDexTool>d8</AndroidDexTool>
		<AndroidDisableMultiDexLegacy>false</AndroidDisableMultiDexLegacy>
		<!-- Enable incremental Java compilation for faster builds -->
		<AndroidEnableIncrementalJava>true</AndroidEnableIncrementalJava>
		<!-- Standard deployment timeout -->
		<DeployTimeout>120000</DeployTimeout>
		<!-- Initial ADB timeout -->
		<AdbInitialOutputWaitTime>60</AdbInitialOutputWaitTime>
		<AndroidProfileOnStart>false</AndroidProfileOnStart>
	</PropertyGroup>

	<!-- Release configuration specific settings -->
	<PropertyGroup Condition="'$(Configuration)' == 'Release'">
		<!-- Embed assemblies into APK for Release builds for stability -->
		<EmbedAssembliesIntoApk>true</EmbedAssembliesIntoApk>
		<!-- Disable Fast Deployment in Release for production-like behavior -->
		<AndroidEnableFastDeployment>false</AndroidEnableFastDeployment>
		<AndroidLinkMode>SdkOnly</AndroidLinkMode>
		<AndroidEnableMultiDex>true</AndroidEnableMultiDex>
		<AndroidDexTool>d8</AndroidDexTool>
	</PropertyGroup>

	<ItemGroup>
		<!-- App Icon - Uncomment when you add icon files -->
		<!-- <MauiIcon Include="Resources\AppIcon\appicon.svg" ForegroundFile="Resources\AppIcon\appiconfg.svg" Color="#4CAF50" /> -->

		<!-- Splash Screen - Uncomment when you add splash file -->
		<!-- <MauiSplashScreen Include="Resources\Splash\splash.svg" Color="#4CAF50" BaseSize="128,128" /> -->

		<!-- Images -->
		<MauiImage Include="Resources\Images\*" />

		<!-- Custom Fonts -->
		<MauiFont Include="Resources\Fonts\*" />

		<!-- Raw Assets (such as ONNX model) -->
		<MauiAsset Include="Resources\Raw\**" LogicalName="%(RecursiveDir)%(Filename)%(Extension)" />
	</ItemGroup>

	<ItemGroup>
	  <MauiAsset Remove="Resources\Raw\mobilenetv2_plantvillage_trained.onnx.data" />
	</ItemGroup>

	<ItemGroup>
	  <Compile Remove="Services\AzureSqlDatabaseService.cs" />
	  <Compile Remove="SyncApi\**\*.cs" />
	  <Compile Remove="**\obj\**\*.cs" />
	  <Compile Remove="**\bin\**\*.cs" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="CommunityToolkit.Maui" Version="7.0.1" />
		<PackageReference Include="CommunityToolkit.Mvvm" Version="8.2.2" />
		<PackageReference Include="Microsoft.Data.SqlClient" Version="7.0.1" />
		<PackageReference Include="Microsoft.Maui.Controls" Version="8.0.3" />
		<PackageReference Include="Microsoft.Maui.Controls.Compatibility" Version="8.0.3" />
		<PackageReference Include="Microsoft.Extensions.Logging.Debug" Version="8.0.0" />
		<PackageReference Include="sqlite-net-pcl" Version="1.9.172" />
		<PackageReference Include="SQLitePCLRaw.bundle_green" Version="2.1.8" />
		<PackageReference Include="Microsoft.ML.OnnxRuntime" Version="1.17.1" />
		<PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
		<PackageReference Include="Plugin.Maui.Audio" Version="2.1.0" />
		
		<!-- Image Processing -->
		<PackageReference Include="SkiaSharp" Version="2.88.7" />
		<PackageReference Include="SkiaSharp.Views.Maui.Controls" Version="2.88.7" />
		
		<!-- Azure AI Services -->
		<PackageReference Include="Azure.AI.OpenAI" Version="2.1.0" />
		<PackageReference Include="Microsoft.CognitiveServices.Speech" Version="1.39.0" />
	</ItemGroup>

	<ItemGroup>
	  <MauiXaml Update="KrishiAI.App\AppShell.xaml">
	    <Generator>MSBuild:Compile</Generator>
	  </MauiXaml>
	  <MauiXaml Update="Views\ForgotPasswordPage.xaml">
	    <Generator>MSBuild:Compile</Generator>
	  </MauiXaml>
	  <MauiXaml Update="Views\HistoryPage.xaml">
	    <Generator>MSBuild:Compile</Generator>
	  </MauiXaml>
	  <MauiXaml Update="Views\LoginPage.xaml">
	    <Generator>MSBuild:Compile</Generator>
	  </MauiXaml>
	  <MauiXaml Update="Views\SignupPage.xaml">
	    <Generator>MSBuild:Compile</Generator>
	  </MauiXaml>
	</ItemGroup>

	<ItemGroup>
	  <Folder Include="KrishiAI.App\Services\" />
	  <Folder Include="KrishiAI.App\ViewModels\" />
	  <Folder Include="KrishiAI.App\Views\" />
	</ItemGroup>

	<!-- Fast Deployment is now enabled for Debug builds for faster iteration -->
	<!-- Removed: DisableFastDeployment target - now using incremental deployment in Debug -->

	<!-- Pre-deployment message for emulator -->
	<Target Name="PreDeploymentInfo" BeforeTargets="Deploy">
		<Message Text="⏳ Starting deployment... The emulator may take 2-5 minutes to fully boot." Importance="high" />
		<Message Text="💡 Tip: If deployment times out, check that your emulator has sufficient RAM (at least 4GB allocated)" Importance="high" />
	</Target>

</Project>
