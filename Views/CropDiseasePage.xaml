<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:KrishiAI.App.ViewModels"
             x:Class="KrishiAI.App.Views.CropDiseasePage"
             x:DataType="viewmodels:CropDiseaseViewModel"
             Title="{Binding CropDiseaseDetectionText}"
             BackgroundColor="#E8F5E9">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Voice" IconImageSource="microphone.png" Command="{Binding NavigateToVoiceCommand}" />
        <ToolbarItem Text="History" IconImageSource="history.png" Command="{Binding NavigateToHistoryCommand}" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">



            <!-- Camera Card -->
            <Frame BackgroundColor="White" 
                   BorderColor="#E0E0E0" 
                   CornerRadius="15"
                   Padding="0" 
                   HasShadow="True"
                   HeightRequest="300">
                <Grid>
                    <!-- Placeholder when no image -->
                    <VerticalStackLayout IsVisible="{Binding HasImage, Converter={StaticResource InvertedBoolConverter}}"
                                       HorizontalOptions="Center" 
                                       VerticalOptions="Center" 
                                       Spacing="15">
                        <Label Text="📷" FontSize="80" HorizontalOptions="Center"/>
                        <Label Text="{Binding TapToCaptureOrSelectText}" 
                               FontSize="16" 
                               TextColor="#666666" 
                               HorizontalOptions="Center"/>
                        <Label Text="{Binding ACropImageText}" 
                               FontSize="16" 
                               TextColor="#666666" 
                               HorizontalOptions="Center"/>
                    </VerticalStackLayout>

                    <!-- Image Display -->
                    <Image Source="{Binding SelectedImagePath}" 
                           IsVisible="{Binding HasImage}"
                           Aspect="AspectFill"/>
                </Grid>
            </Frame>

            <!-- Action Buttons -->
            <VerticalStackLayout Spacing="12">
                <Button Text="{Binding CapturePhotoText}"
                        Command="{Binding CapturePhotoCommand}"
                        BackgroundColor="#4CAF50"
                        TextColor="White"
                        HeightRequest="50"
                        FontSize="16"
                        CornerRadius="10"/>

                <Button Text="{Binding ChooseFromGalleryText}"
                        Command="{Binding PickPhotoCommand}"
                        BackgroundColor="#2196F3"
                        TextColor="White"
                        HeightRequest="50"
                        FontSize="16"
                        CornerRadius="10"/>

                <Button Text="{Binding AnalyzeDiseaseText}"
                        Command="{Binding AnalyzeImageCommand}"
                        IsEnabled="{Binding HasImage}"
                        BackgroundColor="#FF9800"
                        TextColor="White"
                        HeightRequest="55"
                        FontSize="18"
                        FontAttributes="Bold"
                        CornerRadius="10"/>
            </VerticalStackLayout>

            <!-- Loading Indicator -->
            <ActivityIndicator IsRunning="{Binding IsAnalyzing}"
                             IsVisible="{Binding IsAnalyzing}"
                             Color="#4CAF50"
                             HeightRequest="50"/>

            <Label Text="{Binding AnalyzingCropImageText}"
                   IsVisible="{Binding IsAnalyzing}"
                   FontSize="16"
                   TextColor="#4CAF50"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>

            <!-- Results Section -->
            <VerticalStackLayout IsVisible="{Binding HasResult}" Spacing="15">

                <!-- Diagnosis Header -->
                <Label Text="{Binding DiagnosisText}" 
                       FontSize="16" 
                       FontAttributes="Bold" 
                       TextColor="#666666"
                       Margin="0,10,0,0"/>

                <!-- Disease Name and Confidence -->
                <Frame BackgroundColor="White" 
                       BorderColor="#E0E0E0"
                       CornerRadius="15" 
                       Padding="20"
                       HasShadow="True">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="{Binding DetectionResult.DiseaseName}" 
                               FontSize="24" 
                               FontAttributes="Bold" 
                               TextColor="#D32F2F"/>
                        <Label Text="{Binding ConfidenceDisplay}" 
                               FontSize="16" 
                               TextColor="#666666"/>
                        <Frame BackgroundColor="#FFF3E0" 
                               BorderColor="Transparent"
                               CornerRadius="5" 
                               Padding="8,4" 
                               HorizontalOptions="Start"
                               Margin="0,5,0,0">
                            <Label Text="{Binding DetectionResult.Severity, StringFormat='{0} Severity'}" 
                                   FontSize="12" 
                                   FontAttributes="Bold" 
                                   TextColor="#F57C00"/>
                        </Frame>
                    </VerticalStackLayout>
                </Frame>

                <!-- Recommendations Header -->
                <Label Text="{Binding RecommendationsText}" 
                       FontSize="16" 
                       FontAttributes="Bold" 
                       TextColor="#666666"
                       Margin="0,10,0,0"/>

                <!-- Organic Treatment -->
                <Frame BackgroundColor="White" 
                       BorderColor="#E0E0E0"
                       CornerRadius="15" 
                       Padding="20"
                       HasShadow="True"
                       IsVisible="{Binding Recommendation, Converter={StaticResource NotNullConverter}}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="{Binding OrganicTreatmentText}" 
                               FontSize="16" 
                               FontAttributes="Bold" 
                               TextColor="#2E7D32"/>
                        <CollectionView ItemsSource="{Binding Recommendation.OrganicTreatment}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <Label Text="{Binding ., StringFormat='• {0}'}" 
                                           TextColor="#666666" 
                                           FontSize="14" 
                                           Margin="5,2"/>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

                <!-- Chemical Treatment -->
                <Frame BackgroundColor="White" 
                       BorderColor="#E0E0E0"
                       CornerRadius="15" 
                       Padding="20"
                       HasShadow="True"
                       IsVisible="{Binding Recommendation, Converter={StaticResource NotNullConverter}}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="{Binding ChemicalTreatmentText}" 
                               FontSize="16" 
                               FontAttributes="Bold" 
                               TextColor="#FF6F00"/>
                        <CollectionView ItemsSource="{Binding Recommendation.ChemicalTreatment}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <Label Text="{Binding ., StringFormat='• {0}'}" 
                                           TextColor="#666666" 
                                           FontSize="14" 
                                           Margin="5,2"/>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

                <!-- Prevention Tips -->
                <Frame BackgroundColor="White" 
                       BorderColor="#E0E0E0"
                       CornerRadius="15" 
                       Padding="20"
                       HasShadow="True"
                       IsVisible="{Binding Recommendation, Converter={StaticResource NotNullConverter}}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="{Binding PreventionTipsText}" 
                               FontSize="16" 
                               FontAttributes="Bold" 
                               TextColor="#1976D2"/>
                        <CollectionView ItemsSource="{Binding Recommendation.PreventionTips}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <Label Text="{Binding ., StringFormat='• {0}'}" 
                                           TextColor="#666666" 
                                           FontSize="14" 
                                           Margin="5,2"/>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

                <!-- New Analysis Button -->
                <Button Text="{Binding CapturePhotoText}"
                        Command="{Binding ClearResultCommand}"
                        BackgroundColor="#4CAF50"
                        TextColor="White"
                        HeightRequest="50"
                        FontSize="16"
                        FontAttributes="Bold"
                        CornerRadius="10"
                        Margin="0,10,0,0"/>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
